# 🚀 Инструкция по настройке проекта Canban

## ✅ Исправленные проблемы

1. ✅ **ANTHROPIC_API_KEY** - создан файл `.env` с примером настройки
2. ✅ **index.css** - создан файл со стилями и Tailwind директивами
3. ✅ **favicon** - добавлен SVG иконка в `/public/favicon.svg`
4. ✅ **Tailwind CSS** - настроен правильно через PostCSS (убрана CDN версия)
5. ✅ **Vite config** - создан для правильной работы с environment variables

---

## 📋 Шаги для запуска проекта

### 1. Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые пакеты, включая:
- Tailwind CSS (для стилей)
- PostCSS и Autoprefixer (для обработки CSS)
- TypeScript и другие dev зависимости

### 2. Настройка API ключа Anthropic

**ВАЖНО:** Без этого шага AI функции не будут работать!

1. Получите API ключ на https://console.anthropic.com/
2. Откройте файл `.env` в корне проекта
3. Замените `your_anthropic_api_key_here` на ваш реальный API ключ:

```env
VITE_ANTHROPIC_API_KEY=sk-ant-api03-ваш-настоящий-ключ-здесь
```

**Примечание:** В Vite все environment переменные должны начинаться с `VITE_` чтобы быть доступными в браузере.

### 3. Запуск проекта

```bash
npm run dev
```

Проект откроется по адресу: http://localhost:3000

---

## 🔧 Структура проекта

```
canban/
├── .env                    # Environment переменные (НЕ коммитить!)
├── .env.example            # Пример настройки environment
├── index.html              # Главный HTML файл
├── index.css               # Глобальные стили + Tailwind
├── index.tsx               # Точка входа React приложения
├── App.tsx                 # Главный компонент приложения
├── vite.config.ts          # Конфигурация Vite
├── tailwind.config.js      # Конфигурация Tailwind CSS
├── postcss.config.js       # Конфигурация PostCSS
├── public/
│   └── favicon.svg         # Иконка сайта
├── components/             # React компоненты
└── services/               # API сервисы (Claude, Firebase и т.д.)
    └── claudeService.ts    # Сервис для работы с Claude AI
```

---

## ❌ Распространенные ошибки и решения

### Ошибка: "ANTHROPIC_API_KEY is not set"

**Решение:**
1. Убедитесь, что создан файл `.env` (не `.env.example`)
2. Проверьте, что переменная называется `VITE_ANTHROPIC_API_KEY`
3. Перезапустите dev сервер после изменения `.env`

### Ошибка: "Failed to load resource: 404"

**Решение:**
- Все статические файлы (CSS, favicon) теперь созданы
- Убедитесь, что запущен `npm run dev`

### Предупреждение Tailwind CDN

**Решение:**
- CDN версия оставлена для совместимости, но теперь Tailwind установлен локально
- После установки зависимостей (`npm install`) можно убрать CDN из HTML если нужно

---

## 🎨 Дополнительная информация

### Tailwind CSS
Проект использует Tailwind CSS для стилизации. Все утилиты Tailwind доступны в компонентах.

### Шрифты
Подключены следующие шрифты через Google Fonts:
- Inter (основной)
- Bebas Neue
- Merriweather
- Montserrat
- Playfair Display
- Oswald
- Raleway
- Caveat

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что все зависимости установлены: `npm install`
2. Убедитесь, что `.env` файл настроен правильно
3. Перезапустите dev сервер

---

**Готово! 🎉** Теперь проект должен работать без ошибок в консоли.
