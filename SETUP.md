# 🚀 Инструкция по запуску проекта

> PsychoPlan AI Kanban - готов к работе с Claude Sonnet 4!

---

## ✅ Проект полностью настроен

Все критические проблемы исправлены:
- ✅ Миграция с Gemini на Claude завершена
- ✅ Vite конфигурация создана
- ✅ Переменные окружения настроены
- ✅ index.html очищен от старых зависимостей
- ✅ claudeService.ts обновлен для Vite

---

## 📋 Быстрый старт

### 1. Получите API ключ Anthropic

1. Зарегистрируйтесь на https://console.anthropic.com/
2. Перейдите в раздел **API Keys**
3. Создайте новый ключ
4. Скопируйте ключ

### 2. Настройте переменные окружения

Откройте файл `.env.local` и замените placeholder:

```env
VITE_ANTHROPIC_API_KEY=sk-ant-api03-ваш_настоящий_ключ_здесь
```

**ВАЖНО:** Замените `your_anthropic_api_key_here` на ваш настоящий API ключ!

### 3. Установите зависимости

```bash
npm install
```

### 4. Запустите проект

```bash
npm run dev
```

Проект запустится на http://localhost:3000

---

## 🎯 Проверка работы

После запуска:

1. Откройте браузер на http://localhost:3000
2. Нажмите кнопку "AI Generator" (✨ иконка)
3. Попробуйте сгенерировать идеи контента
4. Если всё работает - вы готовы! 🎉

---

## 🐛 Troubleshooting

### Ошибка: "VITE_ANTHROPIC_API_KEY is not set"

**Решение:**
- Проверьте что файл `.env.local` существует в корне проекта
- Убедитесь что ключ начинается с `VITE_`
- Перезапустите dev сервер (Ctrl+C и снова `npm run dev`)

### Ошибка: "AI service is not configured"

**Решение:**
- Проверьте что API ключ правильно указан в `.env.local`
- Убедитесь что ключ валидный (начинается с `sk-ant-`)
- Проверьте баланс аккаунта Anthropic

### Проект не запускается

**Решение:**
```bash
# Удалите node_modules и переустановите зависимости
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## 📦 Команды npm

```bash
npm run dev      # Запуск dev сервера (порт 3000)
npm run build    # Сборка для продакшена
npm run preview  # Предпросмотр prod сборки
```

---

## 🔧 Технические детали

### Используемые технологии
- **React 19.2** - UI framework
- **TypeScript** - Type safety
- **Vite** - Build tool
- **Claude Sonnet 4** - AI generation
- **Firebase** - Backend & Auth
- **Tailwind CSS** - Styling (CDN)

### Структура проекта
```
canban/
├── components/          # React компоненты
├── services/           # API сервисы
│   ├── claudeService.ts   # Claude AI интеграция
│   ├── firebase.ts        # Firebase конфигурация
│   └── storageService.ts  # Локальное хранилище
├── App.tsx             # Главный компонент
├── types.ts            # TypeScript типы
├── vite.config.ts      # Vite конфигурация
├── .env.local          # Переменные окружения (не в git)
└── .env.example        # Пример переменных
```

---

## 🎓 Дополнительные ресурсы

### Документация проекта
- `README.md` - Описание проекта и API
- `MIGRATION_GUIDE.md` - Детали миграции
- `PROJECT_REVIEW.md` - Полный отчет проверки

### Внешние ресурсы
- [Anthropic API Docs](https://docs.anthropic.com/)
- [Claude Console](https://console.anthropic.com/)
- [Vite Documentation](https://vitejs.dev/)

---

## 💡 Советы по использованию

### 1. API ключи в безопасности
- Никогда не коммитьте `.env.local` в git
- Не делитесь API ключом
- Используйте `.env.example` как шаблон

### 2. Стоимость API
- Следите за использованием API на console.anthropic.com
- Claude Sonnet 4 стоит примерно $3 за 1M input токенов
- Установите лимиты в настройках аккаунта

### 3. Разработка
- Hot reload работает автоматически
- Ошибки показываются в консоли браузера
- Используйте React DevTools для отладки

---

## ✨ Готово!

Проект готов к работе. Наслаждайтесь улучшенной генерацией контента с Claude Sonnet 4! 🚀

**Вопросы?** Проверьте MIGRATION_GUIDE.md или README.md
